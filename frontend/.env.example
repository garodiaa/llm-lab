# Server-side Hugging Face API token (required for deployment)
# Get one at https://huggingface.co/settings/tokens
HF_TOKEN=hf_your_token_here

# Optional: Override API base URL (defaults to /api for same-origin)
# NEXT_PUBLIC_API_BASE_URL=/api
